<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generative AI Prompt</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background: linear-gradient(120deg, #f6d365 0%, #fda085 100%);
            font-family: 'Arial', sans-serif;
            color: #fff;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .container {
            margin-top: 50px;
        }
        .card {
            background: rgba(0, 0, 0, 0.8);
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            color: #fff;
            padding: 20px;
            transition: transform 0.3s ease;
        }
        .card:hover {
            transform: scale(1.05);
        }
        .card-header {
            background: #ff6f61;
            color: white;
            font-size: 1.5rem;
            text-align: center;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            padding: 15px;
        }
        .form-control {
    background: rgba(255, 255, 255, 0.9); /* Set background to a more opaque white */
    color: #333; /* Dark text color for contrast */
    border: 1px solid rgba(255, 255, 255, 0.6); /* Light border for a subtle effect */
    border-radius: 10px;
    padding: 10px;
    transition: background 0.3s ease;
}

.form-control:focus {
    background: rgba(255, 255, 255, 1); /* Fully opaque on focus */
    box-shadow: 0 0 8px rgba(255, 111, 97, 0.6); /* Add a shadow for focus effect */
    border-color: #ff6f61; /* Border color matches button color */
}
        .btn-primary {
            background: #ff6f61;
            border-color: #ff6f61;
            width: 100%;
            padding: 10px;
            font-size: 1.2rem;
            border-radius: 10px;
            transition: background 0.3s ease;
        }
        .btn-primary:hover {
            background: #ff8b77;
            border-color: #ff8b77;
        }
        .result-section {
            margin-top: 30px;
        }
        .result-section h2 {
            color: #ff6f61;
        }
        .result-section p {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 10px;
            font-size: 1.1rem;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                Generative AI Prompt
            </div>
            <div class="card-body">
                <form action="/" method="POST">
                    <div class="form-group">
                        <label for="prompt">Enter your prompt:</label>
                        <input type="text" id="prompt" name="prompt" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            </div>
        </div>

        <% if (result) { %>
        <div class="result-section">
            <div class="card">
                <div class="card-body">
                    <h2>Result:</h2>
                    <p><%= result %></p>
                </div>
            </div>
        </div>
        <% } %>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
